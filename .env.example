# MapTiler API key for the Risk Map basemap (https://cloud.maptiler.com).
# Free tier: no credit card required. Create an account, copy your key from
# "Account → API keys", and allow the "Maps" product for it.
# Without a key the map still works using OpenStreetMap raster tiles.
VITE_MAPTILER_KEY=your_key_here
